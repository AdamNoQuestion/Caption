import type { CommandKit } from "commandkit";
import { ActivityType, type Client } from "discord.js";

export default function (bot: Client<true>, handler: CommandKit) {
  // Status does change properly
};